<div class="column">
  <% @sub_categories.where(name: "Caminos de mesa").each do |sub_category| %>
  <%= link_to sub_category_path(sub_category) do  %>
  <div class="card-category-1" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('https://res.cloudinary.com/dmbom1kyl/image/upload/1idhda9lmohqssaqe4fcu7lhxjx2')">
    <div class="text-center">
      <h2>CAMINOS DE MESA</h2>
      <button class="home-button">VER MAS</button>
    </div>
  </div>
  <% end %>
  <% end %>
  <% @categories.where(name: "Cocina").each do |category| %>
  <%= link_to category_path(category) do  %>
  <div class="card-category-2" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url(<%= category.photo %>)">
    <div class="text-center">
      <h2><%= category.name.upcase %></h2>
      <button class="home-button">VER MAS</button>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
<div class="column">
  <% @categories.where(name: "Living").each do |category| %>
  <%= link_to category_path(category) do  %>
  <div class="card-category-2" style="background-image: linear-gradient(rgba(0,0,0,0.3), rgba(0,0,0,0.3)), url(<%= category.photo %> )">
    <div class="text-center">
      <h2><%= category.name.upcase %></h2>
      <button class="home-button">VER MAS</button>
    </div>
  </div>
  <% end %>
  <% end %>
  <% @sub_categories.where(name: "Manteles").each do |sub_category| %>
  <%= link_to sub_category_path(sub_category) do  %>
  <div class="card-category-1" style="background-image: linear-gradient(rgba(0,0,0,0.4), rgba(0,0,0,0.4)), url('https://res.cloudinary.com/dmbom1kyl/image/upload/rlkhaf9y63x1b26ue0h7k4p3lu07')">
    <div class="text-center">
      <h2>Manteles</h2>
      <button class="home-button">VER MAS</button>
    </div>
  </div>
  <% end %>
  <% end %>
</div>
